# 项目开发规范

## 适用范围

- 本文件适用于仓库根目录及其全部子目录。
- 本项目是本地优先的 GitHub Star 仓库洞察工具，当前技术形态为原生 HTML/CSS/JavaScript 前端和 Python 本地服务。除非需求明确要求，不要引入前端框架、构建系统或与现有规模不相称的依赖。
- 项目的已确认版本以 `PROJECT_STATUS.md`、Git tag 或正式发布记录为准。`TODO.md` 中的后续版本仅代表规划，不得当作已发布版本使用。
- 文本文件统一使用 UTF-8；保留现有中文界面语境，通用技术名词可继续使用英文。

## 代码职责与边界

- `starred_repos_dashboard.html` 负责页面结构、设计 token、Tailwind 配置和静态样式。不要把新的业务逻辑直接堆进 HTML。
- `js/app.js` 负责应用启动、模块编排、DOM 事件和跨模块工作流。
- `js/store.js` 负责集中状态管理；新增跨视图状态时优先进入 store，不要散落在多个全局变量中。
- `js/github-api.js` 负责 GitHub API 请求、分页、响应归一化和限流相关信息。
- `js/insights.js` 负责筛选、统计、排序和摘要等纯逻辑。保持其无 DOM 依赖，并继续兼容浏览器全局导出与 Node.js `module.exports`，以便直接测试。
- `js/view.js` 负责渲染和显示格式化。来自 API、README 或用户输入的内容在进入 HTML 前必须转义或经过可靠的安全处理。
- `js/refresh-controller.js` 负责刷新调度与生命周期，不要在其他模块重复实现定时刷新。
- `server.py` 负责本地静态服务和 AI/GitHub 代理接口。新增需要密钥的联网能力时优先放在服务端代理中。
- 在当前无构建架构下，保持浏览器脚本依赖顺序：`github-api.js`、`insights.js`、`security.js`、`store.js`、`refresh-controller.js`、`view.js`、`app.js`。调整全局导出或顺序时必须完整验证启动流程。

## 实现要求

- 优先沿用现有模块和数据结构，保持改动范围与需求相称；只有在确实降低复杂度时才新增抽象。
- 数据计算与筛选规则优先写成纯函数并补充到 `tests/js/insights.test.js`。
- 修改 DOM 结构或元素 ID 时，必须同步检查 `js/app.js`、`js/view.js` 和 `tests/test_dashboard_contract.py` 的引用。
- 不得无意删除当前契约测试覆盖的可读性 token 和关键界面节点。需要改变契约时，应同步更新实现、测试和说明。
- 刷新远端数据时保留已有缓存数据可见，并提供明确的加载、成功、失败和 GitHub API 限流反馈。
- 保留 GitHub 请求的 ETag/304 复用、默认五分钟刷新、低额度延迟、失败退避和本地缓存回退；不得因局部功能改动退化为重复全量请求。
- 保持多账号合并、单账号切换、缓存回退和取消请求等现有行为；网络请求必须考虑超时、失败和用户主动取消。
- 修改 AI 导读时保持 `/api/ai-health` 的 JSON 合约，以及 `/api/ai-guide` 的 SSE `status`、`content`、`[DONE]`、`X-AI-Model`、无 Key mock 模式和客户端取消行为；协议变更必须同时修改服务端、前端和测试。
- 不要默认增加取消 Star、批量删除或其他会修改远端数据的高风险操作。此类功能必须来自明确需求，并提供清晰的范围提示、确认步骤和失败恢复方案。

## 界面规范

- 延续当前 Insight Console 的深色、低噪音、高可读性方向。正文、仓库标题、元数据和标签字号不得低于现有契约，除非设计更新明确改变该约束。
- 交互控件需保持稳定尺寸和清晰状态；检查窄屏与桌面布局，避免文本截断、控件跳动、内容重叠和不可见的 hover/focus 状态。
- 新增交互时补齐加载、空数据、无匹配结果、错误和禁用状态。
- 保持键盘操作可用；不要破坏现有 `/` 聚焦搜索、弹窗关闭和其他已存在的键盘行为。
- 动态内容不得直接拼接未转义的 HTML。若使用 Markdown 渲染，需评估并处理其中可能携带的 HTML 或脚本内容。
- 动态 URL 参数使用 `encodeURIComponent`；新窗口外链使用 `noopener noreferrer`，并校验 URL 来源。

## 安全与本地运行

- 禁止在源码、测试、日志、截图、设计稿或文档中写入真实 GitHub Token、AI API Key、Authorization header 或其他凭证。
- 凭证继续通过未跟踪的 `.env` 或进程环境变量提供；不得提交 `.env`。
- 不得把凭证写入 URL、错误消息或调试输出。新增缓存时只存储完成业务所需的最小数据。
- 服务默认只绑定 `127.0.0.1`。未经明确的部署与安全设计，不得改为对局域网或公网监听。
- 设计效果图和 README 截图必须检查账号、Token、私有仓库、个人信息及其他敏感数据，必要时先脱敏。

## 运行与验证

- 推荐启动方式：

  ```powershell
  .\start_server.bat
  ```

- 也可直接运行：

  ```powershell
  python server.py 8000
  ```

- 通过 `http://127.0.0.1:8000/starred_repos_dashboard.html` 验证页面；涉及 API、AI 导读或 `.env` 的流程不要只用 `file://` 打开 HTML 验证。
- Python 契约与安全测试：

  ```powershell
  python -m unittest discover -s tests
  ```

- JavaScript 纯逻辑测试：

  ```powershell
  node tests/js/insights.test.js
  ```

- 修改共享逻辑、DOM 契约、服务脚本或用户主流程时应运行上述两组测试；无法运行时在交付说明中明确原因和未覆盖风险。
- 新功能和缺陷修复应增加与风险相称的回归测试。视觉更新除自动化测试外，还需在真实浏览器中检查目标桌面和窄屏视口。

## 文档与版本维护

- 功能、配置、启动方式、隐私边界或目录结构发生变化时，同步更新 `README.md`。
- 已完成能力和当前版本发生变化时更新 `PROJECT_STATUS.md`；规划项变化时更新 `TODO.md`。不要把未完成能力写成已交付功能。
- README 中的命令、路径、图片引用和界面文案必须与当前代码一致。

## 设计更新与效果图归档

- 任何涉及界面、交互、布局、视觉样式或设计稿的更新，都必须参考现有归档流程。开始前先查看 `../Designer/GitHub_AccountInfoViewer/README.md` 及相关历史版本，不要脱离既有设计上下文。
- 效果图按截图或来源日期、已确认的产品版本、设计版本或阶段归档到：

  ```text
  ../Designer/GitHub_AccountInfoViewer/YYYY-MM-DD_<产品版本>_<设计版本或阶段>/
  ```

- 不得虚构版本号。产品版本优先取自 `PROJECT_STATUS.md`、Git tag 或正式发布记录；概念稿应明确标记为 `concepts` 等阶段。
- 每次新的设计迭代都创建新的版本目录，不覆盖或悄悄替换历史归档。项目内仍被代码或文档引用的原图应复制到归档，不要移动。
- 每个版本目录必须包含 `MANIFEST.md`，至少记录日期、产品版本、设计版本或阶段、页面/交互状态、文件来源、图片尺寸，以及可确认时的来源提交。设计源文件和未经改写的原始片段放在 `source/`。
- 新增版本目录后，同步更新 `../Designer/GitHub_AccountInfoViewer/README.md` 的归档索引。
- 如果项目 `README.md` 存在界面图片展示，设计更新时必须同步替换所有受影响的图片、Markdown 引用、替代文本和说明文字；不得保留已经过时或与当前界面不一致的预览。当前 README 预览图位于 `docs/images/`，引用路径必须与实际文件一致。
- README 缺少应展示的页面或关键状态时必须补图。能够准确复现时，在真实浏览器中使用代表性数据和固定视口截图；无法准确复现所需页面、数据或交互状态时，向开发者申请由开发者提供精确截图，并说明需要的页面、状态、窗口或视口尺寸、主题和脱敏要求。
- 收到开发者截图后再进行最终 README 替换和版本归档。不得用近似稿、旧截图或无法确认的模拟状态冒充最终效果。
- 完成设计更新后，逐张检查 README 图片可正常显示、归档文件可打开、画面无裁切或重叠，并确认截图不包含敏感信息。
