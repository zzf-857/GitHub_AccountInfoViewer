# GitHub Star Dashboard TODO

## beta_v0.2 迭代目标

`beta_v0.1` 已经完成了需求文档中最核心的视觉重构：大字号、低噪音深色界面、洞察摘要、KPI、语言/主题/活跃图表、仓库卡片列表和基础筛选。

`beta_v0.2` 建议把重点从“看起来更清楚”推进到“更能整理和复盘收藏”：补齐排序、导出、详情、分类、复盘队列和基础个人标注能力。

本轮头脑风暴补充确认：`beta_v0.2` 的顶部固定入口采用 `Portal Cards` 方向，把 GitHub 常见入口做成更显眼、更有探索感的四个入口块，而不是普通按钮。

## 需求对比结论

### 已基本覆盖

- 高可读性深色数据产品风格。
- 清晰的页面层级：侧边筛选、顶部状态、洞察摘要、KPI、图表、仓库列表。
- 关键词、语言、主题、更新时间、Star 数、归档状态、简介类型筛选。
- 空状态、加载骨架、无结果提示。
- 自然语言洞察摘要。
- 仓库卡片列表替代拥挤表格。
- 响应式基础适配。

### 仍未完全覆盖

- 顶部“导出按钮”和“总览 / 洞察 / 仓库列表”视图切换。
- 仓库列表排序：最近、名称、分类、语言、收藏价值等。
- 表格视图 / 卡片视图切换。
- 仓库详情展开或详情抽屉。
- 真正的“分类”体系，目前主要依赖 GitHub topics。
- 双语简介、仅英文简介、高价值仓库、待整理仓库等更贴近需求的指标。
- 点击图表或卡片联动下方列表。
- 筛选栏展开 / 收起。
- 本地备注、标签、已读状态、推荐等级等个人整理能力。
- 导入 / 导出 JSON、CSV、Markdown 的数据闭环。

## beta_v0.2 推荐 TODO

### P0: 应优先做

- [ ] 增加顶部 `Portal Cards` 固定入口。
  - 固定展示 `Explore`、`Topics`、`Trending`、`Collections` 四个 GitHub 常用入口。
  - 视觉上做成“探索传送门”卡片：有独立强调色、短说明、轻量动效和明确 hover 状态。
  - `Explore` 用于发现同类项目或跳转 GitHub Explore。
  - `Topics` 用于进入个人技术主题地图或跳转 GitHub Topics。
  - `Trending` 用于查看 GitHub 热门趋势，并为后续“趋势与个人收藏对比”预留位置。
  - `Collections` 用于沉淀专题集合、复盘列表和个人收藏夹。
  - 第一阶段可先作为外链 / 入口导航实现，后续逐步和本地数据联动。

- [ ] 增加刷新账号数据时的 UI/UX 动效反馈。
  - 点击刷新后按钮进入同步态，避免用户误以为页面卡住。
  - 顶部状态区显示同步进度，例如“正在同步账号 A / 第 2 页 / 已读取 120 个仓库”。
  - 用细进度条、脉冲条或同步轨道展示进行中状态。
  - 刷新时保留旧数据可见，不做整页 loading。
  - 刷新完成后显示本次变化：新增、移除、更新、缓存命中。
  - API 限流或失败时显示明确恢复时间和重试策略。

- [ ] 增加仓库列表排序能力。
  - 支持按更新时间、收藏时间、名称、语言、Star 数、Fork 数排序。
  - 默认排序建议使用“最近收藏优先”，如果缺少 `starred_at` 再回退到 `updated_at`。

- [ ] 增加仓库详情抽屉。
  - 点击仓库卡片后打开右侧详情。
  - 展示 repo 名称、描述、中文自动解读、topics、语言、stars、forks、归档状态、更新时间、收藏时间、来源账号、GitHub 链接。
  - 为后续 README 摘要、license、open issues、本地备注预留位置。

- [ ] 增加 `AI Repo Decoder` 仓库解读模块。
  - 优先放在仓库详情抽屉里，不直接挤进列表，避免信息噪音过高。
  - 输入来源包括 repo 名、description、topics、language、stars、forks、updated_at、starred_at。
  - 为 README 摘要、license、open issues 等后续数据预留字段。
  - 输出结构固定为：
    - 这个仓库是干什么的。
    - 适合什么使用场景。
    - 为什么值得收藏。
    - 可能的同类项目或替代方向。
    - 整理建议，例如“值得复盘”“工具链项目”“可暂存”。
  - v0.2 可先实现 UI、数据结构和 mock 结果。
  - v0.3 再接入 OpenAI API、本地模型或用户自定义 API endpoint。

- [ ] 增加导出按钮。
  - 第一阶段支持导出当前筛选结果。
  - 建议支持 JSON 和 CSV，Markdown 可放到后续。
  - 导出内容应包含当前筛选条件、导出时间和账号来源。

- [ ] 增加“收藏复盘”保存视图。
  - 包含长期未更新、已归档、暂无简介、高 Star 但未整理的仓库。
  - 这个视图比单纯的图表更贴近“我接下来该整理什么”的目标。

- [ ] 增加更明确的分类体系。
  - 不完全依赖 GitHub topics。
  - 先用规则归类：AI/LLM、Frontend、Backend、Data、DevTools、Infra、Design、Learning、Others。
  - 分类结果进入 KPI、筛选项、仓库卡片和洞察摘要。

### P1: v0.2 可选增强

- [ ] 增加本地备注和个人标签。
  - 使用 localStorage 保存，不依赖远端服务。
  - 支持给仓库标注用途、优先级、是否已读、是否值得复盘。

- [ ] 增加卡片视图 / 紧凑表格视图切换。
  - 默认保留卡片视图。
  - 表格视图只展示高密度字段，服务快速排序和批量浏览。

- [ ] 增加图表联动筛选。
  - 点击语言条形图后自动筛选该语言。
  - 点击热门 topic 后自动筛选该 topic。
  - 需要同步更新筛选 chips 和结果摘要。

- [ ] 增加筛选栏折叠能力。
  - 桌面端可收起左侧栏，给列表更多宽度。
  - 窄屏端筛选区改为顶部折叠面板。

- [ ] 增加 API 状态可视化。
  - 显示每个账号的 rate limit、最近同步结果、失败原因、下次重试时间。
  - 限流时不要只显示错误，应给出明确恢复时间。

### P2: 技术整理

- [ ] 清理 `github.py` 命名问题。
  - 当前内容实际是 PowerShell 脚本。
  - 建议改名为 `github.ps1`，或改写成真正的 Python 脚本。

- [ ] 整理旧的 token 缓存逻辑。
  - 当前 `security.js` 仍保留 sessionStorage token 读写，但主流程已经改为读取 `.env`。
  - 如果不再使用页面输入 token，应移除这段旧逻辑，降低误解。

- [ ] 为新增纯逻辑补测试。
  - 分类规则。
  - 排序规则。
  - 导出数据格式。
  - 复盘视图筛选规则。

## beta_v0.2 建议版本边界

建议不要在 v0.2 一次性做 Electron、后端服务、AI 摘要和完整知识库。v0.2 更适合保持当前静态页面架构，在现有基础上补齐高价值交互。

推荐 v0.2 MVP 范围：

1. 顶部 `Portal Cards` 固定入口。
2. 刷新同步反馈。
3. 排序。
4. 详情抽屉。
5. `AI Repo Decoder` UI 与 mock 解读。
6. 导出当前筛选结果。
7. 规则分类。
8. 收藏复盘视图。

完成这些项后，页面就不只是“看 Star 数据”，而是能帮助用户决定“哪些收藏值得整理、哪些技术方向最明显、哪些仓库需要回看”，同时在视觉上形成更有记忆点的产品入口。

## 后续迭代方向

### beta_v0.3: 个人技术收藏库

- 本地备注、个人标签、已读状态、推荐等级。
- `AI Repo Decoder` 接入真实 AI 服务，支持按仓库生成、刷新和缓存解读结果。
- 收藏复盘工作流：待整理、已整理、稍后阅读、重点项目。
- 导入 / 导出完整数据包，便于备份和迁移。
- 更稳定的多账号管理，不再依赖手写 `.env`。

### beta_v0.4: 数据能力升级

- 更准确地区分 `starred_at` 和 `updated_at`。
- 增加收藏趋势、技术兴趣变化、长期未回看仓库分析。
- 增加 README 摘要、license、open issues 等详情数据。
- 增加更可靠的自动分类和主题聚类。

### beta_v0.5: 工程化和桌面化

- 引入 Vite 或类似构建流程，拆分 CSS、状态、视图和业务逻辑。
- 评估 Electron / Tauri 桌面版本。
- 增加本地后端或安全代理，避免浏览器直接读取 `.env` 和直接持有 token。
- 增加端到端测试，覆盖搜索、筛选、排序、导出、详情抽屉和缓存回退。
