# GitHub Star 仓库可视化后台项目状态

## 当前版本

版本：`beta_v0.1`

当前项目是一个本地运行的 GitHub Star 仓库数据可视化后台，主要用于查看个人 GitHub 账号中已 Star 的仓库，并通过更清晰的层级、更大的字号和更低噪音的深色界面，帮助快速理解自己的技术兴趣和收藏结构。

## 当前已完成内容

- 已初始化 Git 仓库，并建立基础忽略规则，避免提交 `.env`、本地编辑器配置、运行日志和截图输出。
- 已将页面重构为 Insight Console 风格，采用现代深色数据产品界面。
- 已放大整体字号：正文、仓库描述、仓库标题、图表标签和 KPI 都更适合长时间阅读。
- 已将旧的密集表格列表改为仓库卡片列表，提升扫读效率。
- 已增加洞察摘要区域，用自然语言说明主语言、高频主题、当前结果集和可补充信息。
- 已增加语言结构图、热门主题区域和最近活跃图表。
- 已增加筛选能力：关键词、语言、主题、更新时间、Star 数、仓库状态、简介类型。
- 已增加保存视图入口：All、AI/LLM、Frontend、DevTools、Data、Recently Starred。
- 已支持 `/` 快捷键聚焦全局搜索框。
- 已增加空状态、加载骨架和筛选结果摘要。
- 已补充前端逻辑测试和页面结构契约测试。
- 已把 GitHub Token 从脚本硬编码改为读取环境变量，降低误提交敏感信息的风险。

## 当前运行方式

在项目根目录运行：

```powershell
python -m http.server 8000 --bind 127.0.0.1
```

然后访问：

```text
http://127.0.0.1:8000/starred_repos_dashboard.html
```

## 当前测试方式

运行 JavaScript 洞察逻辑测试：

```powershell
node tests/js/insights.test.js
```

运行 Python 契约和安全测试：

```powershell
python -m unittest discover -s tests
```

## 后续可以添加的新功能

- 增加仓库详情抽屉，展示 README 摘要、topics、更新时间、license、open issues 等更多信息。
- 增加真正的主题聚类能力，把仓库自动归类为 AI、前端、后端、数据、工具链、基础设施等分组。
- 增加收藏时间趋势分析，区分 `starred_at` 和 `updated_at`，更准确地理解收藏节奏。
- 增加“收藏复盘”视图，列出长期未查看、已归档、无简介但高价值的仓库。
- 增加本地备注和标签能力，允许给仓库添加个人理解、用途和优先级。
- 增加导入/导出功能，支持 JSON、CSV、Markdown 多格式备份。
- 增加多账号管理界面，不再依赖手写 `.env`。
- 增加 GitHub API 限流提示和同步失败重试策略的可视化。
- 增加暗色/亮色主题切换，但默认仍保留当前深色数据产品风格。
- 增加前端构建流程，例如 Vite，把当前静态脚本逐步模块化。
- 增加端到端测试，覆盖搜索、筛选、保存视图、刷新和空状态。
